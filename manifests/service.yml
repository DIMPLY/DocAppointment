apiVersion: v1
kind: Service
metadata:
    name: "docappt"
    labels:
        app: "docappt"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "docappt"